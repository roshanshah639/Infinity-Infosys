import LoginPage from "../../_components/auth/Login";

export const metadata = {
  title: "Login",
};

const LoginRoute = () => {
  return (
    <>
      <LoginPage />
    </>
  );
};

export default LoginRoute;
